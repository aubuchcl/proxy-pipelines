import { ApiResult } from "./request";
import { Settings } from "./settings";
export interface SocketConnectParams<T> {
    target: string;
    token: string;
    settings?: Settings;
    onMessage?: (v: T) => void;
    noJsonDecode?: boolean;
}
export declare function connectToSocket<T>({ target, token, settings, onMessage, noJsonDecode, }: SocketConnectParams<T>): Promise<ApiResult<WebSocket>>;
