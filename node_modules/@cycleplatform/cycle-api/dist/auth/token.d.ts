import { BaseParams, PostParams } from "../common/api";
import { Time } from "../common/structs";
/**
 * An OAuth 2.0 token
 */
export interface Token {
    token: string;
    expires: Time;
}
interface BrowserAccessTokenParams {
    application_id: string;
}
/**
 * Retrieves an access code using the http-only cookie for a direct browser authentication
 */
export declare function getBrowserAccessToken(params: BaseParams & PostParams<BrowserAccessTokenParams>): Promise<import("../common/api").ApiResult<{
    data: Token;
}>>;
interface GrantAccessTokenParams {
    application_id: string;
    application_auth: string;
    grant_code: string;
}
/**
 * Retrieves an access code using the server-side flow
 */
export declare function getGrantAccessToken(params: BaseParams & PostParams<GrantAccessTokenParams>): Promise<import("../common/api").ApiResult<{
    data: Token;
}>>;
export declare function revokeAuth(params: BaseParams): Promise<import("../common/api").ApiResult<unknown>>;
export {};
