import * as Request from "../../../common/api/request";
import { QueryParams, Settings } from "../../../common/api";
import { CollectionDoc, Resource } from "../../../common/structs";
import { Location } from "./location";
export declare type Collection = CollectionDoc<Provider>;
export interface Provider extends Resource<ProviderMetas> {
    name: string;
    identifier: ProviderIdentifier;
    website: string;
    required_fields: Record<string, RequiredField>;
    features: string[];
}
export declare type ProviderIdentifier = "equinix-metal" | "vultr" | "aws";
export interface RequiredField {
    caption: string;
    regex: string;
}
export interface ProviderMetas {
    locations: Location[];
}
export declare function getCollection(params: {
    query?: QueryParams;
    settings?: Settings;
}): Promise<Request.ApiResult<Collection>>;
