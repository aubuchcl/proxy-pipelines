import * as Request from "../../../common/api";
import { StandardParams } from "../../../common/api";
export declare type Single = {
    data: DeploymentStrategy;
};
declare const strategyTypes: readonly ["resource-density", "high-availability", "first-available", "manual", "ephemeral"];
export declare type StrategyTypes = typeof strategyTypes[number];
export declare type DeploymentStrategy = {
    name: string;
    description: string;
};
export declare function getSingle(params: StandardParams): Promise<Request.ApiResult<Single>>;
export {};
