import { Time, Gigabytes } from "../../../common/structs";
import { StandardParams } from "../../../common/api";
export declare type Collection = {
    data: UsageDatum[];
};
export interface UsageDatum {
    time: Time;
    nodes: number;
    memory_gb: Gigabytes;
}
export declare function getServerUsage(params: StandardParams): Promise<import("../../../common/api").ApiResult<Collection>>;
