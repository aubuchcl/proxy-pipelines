import * as Request from "../../../common/api/request";
import { StandardParams } from "../../../common/api";
import { ResourceId, Task, CreatedTask } from "../../../common/structs";
import { Features } from "./server";
export declare type ServerAction = "reconfigure.features";
export declare function reconfigureFeatures(params: StandardParams & {
    id: ResourceId;
    value: Features;
}): Promise<Request.ApiResult<CreatedTask<"reconfigure.features", Features>>>;
export declare function task<K = {}>(params: StandardParams & {
    id: ResourceId;
    value: Task<ServerAction, K>;
}): Promise<Request.ApiResult<CreatedTask<"reconfigure.features", K>>>;
