import * as Request from "../../../common/api/request";
import { StandardParams } from "../../../common/api";
import { ResourceId } from "../../../common/structs";
export interface ConsolePipelineParams extends StandardParams {
    id: ResourceId;
    /** optional typed onmessage handler */
    onMessage?: (v: string) => void;
}
export interface ConsolePipelineResponse {
    data: {
        token: string;
        address: string;
    };
}
export declare function connectToConsole(params: ConsolePipelineParams): Promise<Request.ApiResult<WebSocket>>;
