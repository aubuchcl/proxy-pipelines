import * as Request from "../../common/api/request";
import { StandardParams } from "../../common/api";
import { Resource, ResourceId, SingleDoc } from "../../common/structs";
export declare type Single = SingleDoc<Response>;
export interface Response extends Resource {
    survey_id: string;
    account_id: string;
    responses: Responses;
}
export declare type Responses = Record<string, ResponseSection>;
export declare type ResponseSection = {
    answers: Record<string, ResponseAnswer>;
};
export declare type ResponseAnswer = {
    value: string;
    extra: Record<string, string>;
};
export declare type postResponseParams = {
    survey_id: string;
    account_id: string;
    responses: Responses;
};
export declare function postResponse(params: StandardParams & {
    value: Response;
}): Promise<Request.ApiResult<Response>>;
export declare function getResponse(params: StandardParams & {
    survey_id: ResourceId;
}): Promise<Request.ApiResult<Response>>;
