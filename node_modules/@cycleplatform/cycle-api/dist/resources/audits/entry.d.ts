import * as Request from "../../common/api/request";
import { StandardParams } from "../../common/api";
import { CollectionDoc, Resource, Events, UserScope, ResourceId, SingleDoc, IP } from "../../common/structs";
export interface EntrySession {
    ip: IP;
    scope: string[];
}
export declare type EntryType = "success";
export declare enum EntryComponentType {
    ENVIRONMENT = "environments.Environment"
}
export declare enum EntryCode {
    ENVIRONMENT_CREATED = "environment.created"
}
export interface EntryComponent {
    type: EntryComponentType;
    id: ResourceId;
}
export interface Entry extends Resource {
    hub_id: ResourceId;
    environment_id: ResourceId;
    creator: UserScope;
    session: EntrySession;
    type: EntryType;
    events: Events;
    component: EntryComponent;
    message: string;
    code: EntryCode;
}
export declare type Collection = CollectionDoc<Entry>;
export declare type Single = SingleDoc<Entry>;
export declare function getEntries(params: StandardParams): Promise<Request.ApiResult<Collection>>;
