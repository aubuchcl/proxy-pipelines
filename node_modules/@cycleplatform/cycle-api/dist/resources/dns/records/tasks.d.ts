import * as Request from "../../../common/api/request";
import { StandardParams } from "../../../common/api";
import { ResourceId, Task, CreatedTask } from "../../../common/structs";
export declare type RecordAction = "cert.generate";
export declare function generateCert(params: StandardParams & {
    id: ResourceId;
    zoneId: ResourceId;
}): Promise<Request.ApiResult<CreatedTask<"cert.generate", {}>>>;
export declare function task(params: StandardParams & {
    id: ResourceId;
    zoneId: ResourceId;
    value: Task<RecordAction>;
}): Promise<Request.ApiResult<CreatedTask<"cert.generate", {}>>>;
