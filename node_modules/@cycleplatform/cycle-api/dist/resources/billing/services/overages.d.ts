import { StandardParams } from "../../../common/api";
import * as Request from "../../../common/api/request";
import { Term } from "../term";
export interface Overage {
    term: Term;
    ram: {
        gb_hours: number;
        cost: number;
    };
}
export declare function getOverages(params: StandardParams): Promise<Request.ApiResult<{
    data: Overage;
}>>;
