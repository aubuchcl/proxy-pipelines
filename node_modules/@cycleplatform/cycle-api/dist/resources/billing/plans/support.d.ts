import * as Request from "../../../common/api/request";
import { QueryParams, Settings } from "../../../common/api";
import { CollectionDoc, Resource, SingleDoc } from "../../../common/structs";
import { Amount } from "../../billing";
export declare type Collection = CollectionDoc<SupportPlan>;
export declare type Single = SingleDoc<SupportPlan>;
export interface SupportPlan extends Resource {
    name: string;
    price: Amount;
    description: string;
    features: Features;
    contract: boolean;
    default?: true;
}
export interface Features {
    engineering_support: boolean;
    uptime_sla: boolean;
    guaranteed_response_time: string | null;
}
export declare function getCollection(params: {
    query?: QueryParams;
    settings?: Settings;
}): Promise<Request.ApiResult<Collection>>;
