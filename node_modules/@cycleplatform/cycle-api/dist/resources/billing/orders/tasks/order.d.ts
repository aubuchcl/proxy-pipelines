import { ResourceId, Task, CreatedTask } from "../../../../common/structs";
import * as Request from "../../../../common/api/request";
import { StandardParams } from "../../../../common/api";
export declare type OrderAction = "confirm";
export declare function confirm(params: StandardParams & {
    id: ResourceId;
}): Promise<Request.ApiResult<CreatedTask<"confirm", {}>>>;
export declare function task(params: StandardParams & {
    id: ResourceId;
    value: Task<OrderAction>;
}): Promise<Request.ApiResult<CreatedTask<"confirm", {}>>>;
