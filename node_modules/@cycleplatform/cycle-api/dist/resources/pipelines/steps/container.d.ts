import { Config, Volumes } from "../../containers/config";
import { ResourceLocation } from "./shared-types";
export interface Create {
    name: string;
    environment: ResourceLocation;
    image: ResourceLocation;
    stateful: boolean;
    annotations: Record<string, any>;
    config: Config;
    volumes?: Volumes.Volume[];
}
export declare type Start = ResourceLocation;
export declare type Stop = ResourceLocation;
export declare type Restart = ResourceLocation;
export declare type Reimage = ResourceLocation & {
    image: ResourceLocation;
};
export declare type Delete = ResourceLocation;
