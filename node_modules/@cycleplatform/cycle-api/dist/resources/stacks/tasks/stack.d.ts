import * as Request from "../../../common/api/request";
import { StandardParams } from "../../../common/api";
import { ResourceId, Task, CreatedTask } from "../../../common/structs";
export declare type StackAction = "prune";
/** Removes all builds that are not in use and have not been created in the last 30 minutes */
export declare function pruneBuilds(params: StandardParams & {
    id: ResourceId;
}): Promise<Request.ApiResult<CreatedTask<"prune", {}>>>;
export declare function remove(params: StandardParams & {
    id: ResourceId;
}): Promise<Request.ApiResult<CreatedTask<"delete", {}>>>;
export declare function task<K = {}>(params: StandardParams & {
    id: ResourceId;
    value: Task<StackAction, K>;
}): Promise<Request.ApiResult<CreatedTask<"prune", K>>>;
