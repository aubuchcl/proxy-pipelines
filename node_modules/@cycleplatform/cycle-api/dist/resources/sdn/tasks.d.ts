import * as Request from "../../common/api/request";
import { StandardParams } from "../../common/api";
import { ResourceId, CreatedTask, Task } from "../../common/structs";
export declare type NetworkAction = "reconfigure";
export interface ReconfigureParams {
    environment_ids: ResourceId[];
}
export declare function reconfigure(params: StandardParams & {
    id: ResourceId;
    value: ReconfigureParams;
}): Promise<Request.ApiResult<CreatedTask<"reconfigure", ReconfigureParams>>>;
export declare function task<K = {}>(params: StandardParams & {
    id: ResourceId;
    value: Task<NetworkAction, K>;
}): Promise<Request.ApiResult<CreatedTask<"reconfigure", K>>>;
