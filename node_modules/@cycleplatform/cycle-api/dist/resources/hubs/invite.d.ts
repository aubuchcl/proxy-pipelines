import * as Request from "../../common/api/request";
import { StandardParams } from "../../common/api";
import { CollectionDoc, SingleDoc, Email, ResourceId } from "../../common/structs";
import * as Memberships from "./membership";
/**
 * Information on the accounts role
 */
export declare type RoleName = "owner" | "admin" | "developer" | "analyst";
/**
 * Hub invite information used fro creating an invite
 */
export interface CreateParams {
    recipient: Email;
    role: RoleName;
}
export declare function remove(params: StandardParams & {
    inviteId: ResourceId;
}): Promise<Request.ApiResult<SingleDoc<Memberships.Membership, {}, {}>>>;
export declare function getCollection(params: StandardParams<Memberships.MembershipQuery>): Promise<Request.ApiResult<CollectionDoc<Memberships.Membership, {}, {}>>>;
export declare function create(params: StandardParams<Memberships.MembershipQuery> & {
    value: CreateParams;
}): Promise<Request.ApiResult<SingleDoc<Memberships.Membership, {}, {}>>>;
