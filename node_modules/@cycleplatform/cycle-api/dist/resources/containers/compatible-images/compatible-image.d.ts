import { ResourceId, CollectionDoc } from "../../../common/structs";
import { StandardParams } from "../../../common/api";
import * as Request from "../../../common/api/request";
import { Image } from "../../images";
export declare type Collection = CollectionDoc<Image>;
export declare function getCompatibleImages(params: StandardParams & {
    id: ResourceId;
}): Promise<Request.ApiResult<Collection>>;
