import { Time, ResourceId } from "../../../common/structs";
import { StandardParams } from "../../../common/api";
import { InstanceState } from "../instances";
import * as Request from "../../../common/api/request";
export declare type Collection = {
    data: TelemetryPoint[];
};
/** A single telemetry point for an instance */
export interface TelemetryPoint {
    time: Time;
    instances: Record<InstanceState, number>;
}
export declare function getInstancesTelemetry(params: StandardParams & {
    containerId: ResourceId;
}): Promise<Request.ApiResult<Collection>>;
